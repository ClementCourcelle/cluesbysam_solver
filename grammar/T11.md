
| "Only one" | AXIS   | "has" | NB  | ROLE      |
| ---------- | ------ | ----- | --- | --------- |
| Only one   | Column | has   | 2   | criminals |
SLICE(axis(1), axis($\Omega$(axis)))

(AXIS(1) : nb r && AXIS(1_bar) !=nb) | 
(AXIS(2) : nb r && AXIS(2_bar) !=nb) |
...

OR(SLICE(
	AXIS(1) : nb r && AXIS(1_bar) !=nb,
	truc
))